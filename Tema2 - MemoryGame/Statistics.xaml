<Window x:Class="Tema2___MemoryGame.Statistics"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tema2___MemoryGame"
        mc:Ignorable="d"
        Title="Statistics" Height="450" Width="680">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Games Played" Binding="{Binding GamesPlayed}" Width="*"/>
                <DataGridTextColumn Header="Games Won" Binding="{Binding GamesWon}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
